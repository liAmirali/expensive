{
  "name": "expensive",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter server start:dev",
    "dev:client": "pnpm --filter client dev",
    "gen:api": "pnpm --filter server gen:openapi && pnpm --filter client gen:api"
  }
}
