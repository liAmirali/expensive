import { body } from "express-validator";

export const addExpenseValidators = [
  // body("value").trim().isFloat({ min: 0 }),
  // body("description").optional().trim().escape(),
  // body("category").optional().trim(),
  // body("currency").optional().isCurrency(),
];
